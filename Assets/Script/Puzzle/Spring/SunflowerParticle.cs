using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SunflowerParticle : MonoBehaviour
{
    private ParticleSystem particleSyt;

    // Start is called before the first frame update
    void Start()
    {
        particleSyt = GetComponent<ParticleSystem>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
